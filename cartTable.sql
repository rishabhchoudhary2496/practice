-- create table cart(
-- 	cart_id int,
--     user_id int,
--     product_id int,
-- 	   quantity int,
--     price decimal(10,2) default 0,
--     foreign key(user_id) references user(user_id),
--     foreign key(product_id) references product(product_id)

-- )

-- insert into cart values(101,3,3,1,30000);
#insert into cart values(101,3,2,1,40000);
